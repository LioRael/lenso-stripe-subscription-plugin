{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["event_id", "event_type", "outcome", "reconciliation_enqueued"],
  "properties": {
    "event_id": {"type": "string", "minLength": 1, "maxLength": 255},
    "event_type": {"type": "string", "minLength": 1, "maxLength": 255},
    "outcome": {"type": "string", "enum": ["accepted", "duplicate", "ignored"]},
    "reconciliation_enqueued": {"type": "boolean"}
  },
  "additionalProperties": false,
  "title": "IngestWebhookResponse"
}

